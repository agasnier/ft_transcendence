{
  "db_name": "mariadb",
  "creation_statements": [
    "CREATE USER '{{name}}'@'%' IDENTIFIED BY '{{password}}';",
    "GRANT CREATE ON `${MARIADB_DATABASE}`.* TO '{{name}}'@'%';",
    "GRANT SELECT, INSERT, UPDATE, DELETE, CREATE, ALTER, DROP, INDEX, REFERENCES ON `${MARIADB_DATABASE}`.`api_keys` TO '{{name}}'@'%';",
    "GRANT SELECT, INSERT, UPDATE, DELETE, CREATE, ALTER, DROP, INDEX, REFERENCES ON `${MARIADB_DATABASE}`.`__drizzle_migrations_api` TO '{{name}}'@'%';"
  ],
  "default_ttl": "24h",
  "max_ttl": "72h"
}
