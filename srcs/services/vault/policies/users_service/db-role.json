{
  "db_name": "mariadb",
  "creation_statements": "CREATE USER '{{name}}'@'%' IDENTIFIED BY '{{password}}'; GRANT ALL PRIVILEGES ON `${MARIADB_DATABASE}`.* TO '{{name}}'@'%';",
  "default_ttl": "24h",
  "max_ttl": "72h"
}
